# MNIST-Classification
A PyTorch-based Convolutional Neural Network (CNN) for MNIST handwritten digit classification, achieving high accuracy with dropout regularization, flexible architecture, and built-in training/evaluation with loss and accuracy visualization.
